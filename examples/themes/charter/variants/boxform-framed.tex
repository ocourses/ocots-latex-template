% Variante : theme=charter + boxform=framed (voir la note affichée dans le PDF).
\documentclass[11pt]{ocots-book}
\usepackage[lang=fr, theme=charter, boxform=framed, solutions=inline, math=analysis]{ocots}
\lstset{language=[LaTeX]TeX}

\title{Variante d'option}
\author{Prénom \textsc{Nom}}

\newcommand{\ocotsvariantnote}[1]{\begin{quote}\itshape Variante : #1\end{quote}}

\begin{document}
\maketitle

\begin{lstlisting}
\usepackage[..., theme=charter, boxform=framed]{ocots}
\end{lstlisting}
\ocotsvariantnote{\texttt{boxform=framed} (le th\`eme \texttt{charter} utilise par d\'efaut \texttt{sidebar}).}
\input{../../../content/variant-body}
\end{document}
