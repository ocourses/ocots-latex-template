% Variante : theme=ocots + mathbox=rule (voir la note affichée dans le PDF).
\documentclass[11pt]{ocots-book}
\usepackage[lang=fr, theme=ocots, mathbox=rule, solutions=inline, math=analysis]{ocots}
\lstset{language=[LaTeX]TeX}

\title{Variante d'option}
\author{Prénom \textsc{Nom}}

\newcommand{\ocotsvariantnote}[1]{\begin{quote}\itshape Variante : #1\end{quote}}

\begin{document}
\maketitle

\begin{lstlisting}
\usepackage[..., theme=ocots, mathbox=rule]{ocots}
\end{lstlisting}
\ocotsvariantnote{\texttt{mathbox=rule} (le th\`eme \texttt{ocots} utilise par d\'efaut \texttt{highlight}).}
\input{../../../content/variant-body}
\end{document}
