% Variante : theme=ocots + titles=rules (voir la note affichée dans le PDF).
\documentclass[11pt]{ocots-book}
\usepackage[lang=fr, theme=ocots, titles=rules, solutions=inline, math=analysis]{ocots}
\lstset{language=[LaTeX]TeX}

\title{Variante d'option}
\author{Prénom \textsc{Nom}}

\newcommand{\ocotsvariantnote}[1]{\begin{quote}\itshape Variante : #1\end{quote}}

\begin{document}
\maketitle

\begin{lstlisting}
\usepackage[..., theme=ocots, titles=rules]{ocots}
\end{lstlisting}
\ocotsvariantnote{\texttt{titles=rules} (le th\`eme \texttt{ocots} utilise par d\'efaut \texttt{plain}).}
\input{../../../content/variant-body}
\end{document}
