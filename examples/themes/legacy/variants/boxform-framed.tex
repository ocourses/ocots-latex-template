% Variante : theme=legacy + boxform=framed (voir la note affichée dans le PDF).
\documentclass[11pt]{ocots-book}
\usepackage[lang=fr, theme=legacy, boxform=framed, solutions=inline, math=analysis]{ocots}
\lstset{language=[LaTeX]TeX}

\title{Variante d'option}
\author{Prénom \textsc{Nom}}

\newcommand{\ocotsvariantnote}[1]{\begin{quote}\itshape Variante : #1\end{quote}}

\begin{document}
\maketitle

\begin{lstlisting}
\usepackage[..., theme=legacy, boxform=framed]{ocots}
\end{lstlisting}
\ocotsvariantnote{\texttt{boxform=framed} (r\'eglage par d\'efaut du th\`eme \texttt{legacy} : la variante force explicitement la valeur).}
\input{../../../content/variant-body}
\end{document}
