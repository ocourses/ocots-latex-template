% Variante : theme=slate + mathbox=flat (voir la note affichée dans le PDF).
\documentclass[11pt]{ocots-book}
\usepackage[lang=fr, theme=slate, mathbox=flat, solutions=inline, math=analysis]{ocots}
\lstset{language=[LaTeX]TeX}

\title{Variante d'option}
\author{Prénom \textsc{Nom}}

\newcommand{\ocotsvariantnote}[1]{\begin{quote}\itshape Variante : #1\end{quote}}

\begin{document}
\maketitle

\begin{lstlisting}
\usepackage[..., theme=slate, mathbox=flat]{ocots}
\end{lstlisting}
\ocotsvariantnote{\texttt{mathbox=flat} (r\'eglage par d\'efaut du th\`eme \texttt{slate} : la variante force explicitement la valeur).}
\input{../../../content/variant-body}
\end{document}
